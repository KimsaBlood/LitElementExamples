{
  "scripts": {
    "build": "rimraf dist && rollup -c rollup.config.js",
    "start:build": "npm run build && web-dev-server --root-dir dist --app-index index.html --open --compatibility none",
    "start": "web-dev-server"
  },
  "devDependencies": {
    "@open-wc/building-rollup": "^1.10.0",
    "deepmerge": "^4.2.2",
    "rimraf": "^3.0.2",
    "rollup": "^2.52.2",
    "@web/dev-server": "^0.1.17"
  },
  "name": "redux-examples",
  "version": "0.0.0",
  "description": "Webcomponent redux-examples following open-wc recommendations",
  "author": "redux-examples",
  "license": "MIT",
  "dependencies": {
    "lit-element": "^2.5.1",
    "lit-html": "^1.4.1",
    "pwa-helpers": "^0.9.1",
    "redux": "^4.1.0"
  }
}
